const { PI, min, max } = Math;

const rnd2 = (a) => Math.round(a * 100) / 100;
const rnd3 = (a) => Math.round(a * 100) / 100;

export { default as clip_rgb } from 'chroma-js/src/utils/clip_rgb.js';
export { default as limit } from 'chroma-js/src/utils/limit.js';
export { default as type } from 'chroma-js/src/utils/type.js';
export { default as unpack } from 'chroma-js/src/utils/unpack.js';
export { default as last } from 'chroma-js/src/utils/last.js';

const TWOPI = PI * 2;
const PITHIRD = PI / 3;
const DEG2RAD = PI / 180;
const RAD2DEG = 180 / PI;

export { PI, TWOPI, PITHIRD, DEG2RAD, RAD2DEG, min, max, rnd2, rnd3 };
