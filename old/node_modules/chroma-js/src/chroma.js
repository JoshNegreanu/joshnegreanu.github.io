import Color from 'chroma-js/src/Color';
import { version } from 'chroma-js/src/version.js';

const chroma = (...args) => {
    return new Color(...args);
};

chroma.version = version;

export default chroma;
